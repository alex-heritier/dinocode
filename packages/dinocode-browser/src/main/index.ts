/**
 * Electron main-process entry point for the built-in browser.
 *
 * This module will export the BrowserManager service, the CDP adapter, the
 * tab registry, and the IPC handlers. It runs ONLY in the Electron main
 * process and may import `electron` freely.
 *
 * Kept intentionally empty at scaffold time — real code lands in:
 *   • dinocode-ousa  (BrowserManager service)
 *   • dinocode-u1nj  (CDPAdapter)
 *   • dinocode-cnnp  (IPC schema / preload bridge)
 */

export {};

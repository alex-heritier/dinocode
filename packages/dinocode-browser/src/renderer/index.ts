/**
 * Renderer (React) entry for the built-in browser.
 *
 * Exports the <BrowserPanel /> component, hooks, and stores that the web app
 * embeds. This module assumes a DOM + React runtime; it must not be imported
 * from the server or the CLI.
 *
 * Scaffold only — concrete panel lives in dinocode-qb85.
 */

export {};
